#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int tc; cin >> tc;
    while (tc--) {
        int n;
        ll x;
        cin >> n >> x;
        vector<ll> a;

        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
    }
    return 0;
}
